@page "/player"

@inherits Fluxor.Blazor.Web.Components.FluxorComponent

<PlayerComponent OnNextClickCallback="@(async () => await NextSongAsync())"
                 OnPreviousClickCallback="@(async () => await PreviousSongAsync())" 
                 IsBeginList="@_isBeginList"
                 IsEndList="@_isEndList"
                 @ref="_playerComponent"  
                 OnShowMusicListClickCallback="@(() => MusicListDisplayShuffle())" 
                 IsDisplay="@(!_isMusicListDisplay)" />

<MusicListComponent OnClickCallback="@( async (song) => { await OnSelectedSong(song); })" 
                    IsDisplay="@_isMusicListDisplay" />